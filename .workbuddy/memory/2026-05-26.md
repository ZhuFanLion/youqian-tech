# 2026-05-26

## FLUX.1 Kontext Dev VTON 工作流

- 废弃了之前的 Gemini API 网格工作流（`templates-fashion_shoot_vton.json`），转用 FLUX.1 Kontext Dev 多参考图方案
- 生成了新的工作流 `C:/Users/admin/Downloads/fashion_vton_kontext.json`
- 架构：7张参考图（3服装 + 2人物 + 2配饰）→ RMBG去背景 → 统一768² → ImageBatchMulti → FluxKontextImageScale → VAEEncode → ReferenceLatent → FluxGuidance → KSampler 2K输出
- 输出分辨率：1536×2048 (2K 3:4)
- 模型：flux1-dev-kontext_fp8_scaled.safetensors
- 生成脚本：`C:/Users/admin/WorkBuddy/Claw/build_vton_workflow.py`（可调参数重新生成）
- 待确认：ComfyUI Cloud 是否支持 FluxGuidance/ReferenceLatent 等 Kontext 核心节点
- **确认结果：Cloud 不支持 Kontext 节点 + 模型缺失 → 转向 Cloud 兼容方案**

## FLUX.1 Dev VTON 工作流 - Cloud 兼容版

- 确认 ComfyUI Cloud (v0.22.2) 不支持 Kontext 节点体系（FluxGuidance/ReferenceLatent/FluxKontextImageScale/LoadDiffusionModel）
- 确认 Cloud 缺失 flux1-dev-kontext 模型，无法上传自定义 UNet 模型
- 重写工作流为 Cloud 兼容版：`C:/Users/admin/Downloads/fashion_vton_cloud.json`
- 生成脚本：`C:/Users/admin/WorkBuddy/Claw/build_vton_cloud.py`
- 核心改动：
  - LoadDiffusionModel → UNETLoader
  - 去除所有 Kontext 节点，去除 cnr_id 属性
  - 新增 img2img 路径：服装图1 → VAEEncode → LatentUpscale → KSampler(denoise=0.85)
  - 使用 DualCLIPLoader(type="flux") + EmptySD3LatentImage → LatentUpscale 替代
- 架构：UNETLoader + DualCLIPLoader + VAELoader → 7图参考预览 → 服装图1 VAEEncode → LatentUpscale(1536×2048) → KSampler img2img → VAEDecode → 输出
- 总节点数：33，连接数：34
- 待测试：Cloud 是否识别 UNETLoader/DualCLIPLoader/ImageScale/ImageBatch/LatentUpscale 等节点类型
- **第2轮**：改用 CheckpointLoaderSimple + flux1-dev.safetensors，Cloud 无此模型 → 缺模型(1)
- **第3轮**：Cloud 无原版 flux1-dev，只找到风格化微调模型（flux2-klein-9b、flux1-photorealtouch 等）
- 改用 `flux1-photorealtouch.safetensors`（真实质感风格，最接近 VTON 目标）
- 节点类型全部通过，仅等模型名匹配 + 上传参考图
- **第4轮**：photorealtouch 仍缺模型 — Cloud 的 FLUX 模型在 diffusion_models，不在 checkpoints
- 切回 UNETLoader + DualCLIPLoader + VAELoader 分离加载
- 新增自动递增序号功能：`fashion_vton_cloud_v{N}.json`，不覆盖旧文件
- 输出：`C:/Users/admin/Downloads/fashion_vton_cloud_v1.json` (33节点, 34连接)
- **第5轮 (v2)**：凡凡确认 Cloud checkpoints 库有 Comfy-Org/flux1-dev - flux1-dev-fp8
- 改回 CheckpointLoaderSimple + `flux1-dev-fp8.safetensors`（31节点, 34连接）
- 输出：`C:/Users/admin/Downloads/fashion_vton_cloud_v2.json`
- **v3/v4**: 凡凡提供官方 Gemini 工作流 JSON → 学习语法思路重构 FLUX 方案
  - 5 项改进: GetImageSize+ResizeAndPadImage 自动尺寸匹配、PrimitiveStringMultiline 独立提示词、2 图代替 7 图、ImageBatch+ImageFromBatch、整洁列布局
  - 输出：`C:/Users/admin/Downloads/fashion_vton_cloud_v4.json` (22节点, 26连接)
  - 生成脚本更新: `build_vton_cloud.py`

## ComfyUI 便携版重装

- 旧版 ComfyUI (`E:\ComfyUI_windows_portable\`) 存在多处问题（ComfyUI_Seg_VITON 路径冲突、huggingface_hub 下载失败等），凡凡决定重装
- 已下载 ComfyUI v0.22.0 NVIDIA 便携版至 `C:\Users\admin\Downloads\ComfyUI_windows_portable_nvidia.7z`（1.87GB，SHA 与 GitHub 一致）
- 来源：ghfast.top 镜像加速，耗时 1 分 49 秒（平均约 17MB/s）
- 注意：仓库已从 comfyanonymous 迁移至 Comfy-Org/ComfyUI
- 待办：解压到 E 盘，迁移旧版中的自定义节点和模型

## FitDiT ComfyUI 节点安装

- 仓库：BoyuanJiang/FitDiT（FitDiT-ComfyUI 分支），基于 SD3 DiT 架构的高保真虚拟试衣
- 克隆到 `E:\ComfyUI_windows_portable\ComfyUI\custom_nodes\FitDiT\`
- 下载模型文件至 `models/FitDiT_models/` 和 `models/clip/`：
  - FitDiT 主模型：transformer_garm (3.6GB) + transformer_vton (3.6GB) + vae (160MB)
  - 预处理模型：dwpose (244MB) + humanparsing (278MB) + pose_guider (9.8MB)
  - CLIP 模型：clip-vit-large-patch14 (1.6GB .bin) + CLIP-ViT-bigG-14 (9.5GB .bin)
  - **总计约 18.5GB，全部通过浏览器从 hf-mirror 手动下载**
- **关键发现：ComfyUI v0.22.0 实际使用全局 Python 3.14（非嵌入式 Python 3.13）**
  - `system_stats` API 确认：`python_version: 3.14.5, embedded_python: false`
  - 依赖需安装到全局 Python：`pip install diffusers accelerate opencv-python matplotlib`
- 清理了 3 份重复安装（FitDiT/、FitDiT-ComfyUI/、comfyui-fitdit/），仅保留 FitDiT/
- 3 个节点可用：Load FitDiT Model、Generate FitDiT Mask、FitDiT Try-On
- Manager 列表显示问题（已知 bug），画布搜索正常
